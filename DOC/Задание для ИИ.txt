Создать программу приема пакетов ADS-B в Arduino IDE. Микроконтроллер RP2040. 
Входной сигнал pin 19 и pin 22. Использовать функцию irq на входе. 
Индикация приема преамбулы pin 20. Преобразовать принятый пакет в формат hex и отправить в UART2.
UART_TX_PIN 4.  UART_RX_PIN  5. Применить фильтр помех.  FreeRTOS не применять.

Создать программу приема пакетов ADS-B в Arduino IDE. Микроконтроллер RP2040. 
Входной сигнал pin 19. Использовать функцию irq на входе. 
Индикация приема преамбулы pin 20. Преобразовать принятый пакет в формат hex и отправить в UART2.
UART_TX_PIN 4.  UART_RX_PIN  5. FreeRTOS не применять.

Прием пакета ADS-B. Преобразовать данный код для приема пакетов в Arduino IDE. микроконтроллер RP2040


Создать программу приема пакетов ADS-B в Arduino IDE. Микроконтроллер RP2040. FreeRTOS не применять.
Входной сигнал pin 19 и pin 22. Использовать функцию irq на входе. Применить буффер DMA приема пакета.
Создать входные очереди принятых пакетов. Задействовать 2 ядра. Прием пакетов ядро 1. Обработка пакетов ядро 0.
Индикация приема preamble detector pin 20. pin 23. Полностью расшифровать принятый пакет.
Расшифрованные данные типа icao, номер рейса, latitude, longitude, скорость, высоту отправить в UART2.
UART_TX_PIN 4.  UART_RX_PIN  5. Применить фильтр помех.  
Пакеты 56 бит и 112 бит преобразовать в байты, отправлять в Serial до проверки контроля CRC отладочная функция
Создать функцию регулировки усиления. Входной сигнал подается на pin 26.
Выходной сигнал шим на pin 9. Контроль уровня сигнала управления усиления pin 27.
Контроль целосности пакета CRC. Сформировать таблицу CRC 256 байт.
Коррекция контрольной суммы. Если обнаружена ошибка в одном бите: переворачиваем ее и помещаем исправленный пакет в выходную очередь.
Периодичесски включать pin 15 на ядре 0. Частота переключения 1000 миллисекунд.
Периодичесски включать pin 25 на ядре 1. Частота переключения 500 миллисекунд.